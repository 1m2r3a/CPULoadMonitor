// CPUUsageConsole.cpp : Defines the entry point for the console application.
//
/*
#include "Query.h"
#include "Helper.h"


int main()
{
	Query query;
	query.Init();
	printf("Number of processors: %ld\n", GetProcessorCount());
	vector<PCTSTR> validnames = GetValidCounterNames();
	for (auto it = validnames.begin(); it != validnames.end(); it++)
	{
		printf("%ls\n", *it);
	}
	query.AddCounterInfo(validnames.at(0));
	while(1)
	query.Record();
    return 0;
}
*/
